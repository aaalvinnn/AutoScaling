# 二次审稿意见整理（仅师兄补充部分）

> 本文件只提取合作师兄在二次意见中**带括号补充中文说明**的若干点（共 6 点 + 1 条通用要求），并附原审稿意见作为背景，供后续实验与论文修改参考。

---

## ① 第一组 · 意见 1 —— 大规模下动作空间可扩展性

**原审稿意见：** MDRL 算法在小规模实验中表现优异，但其扁平动作空间 `(server, microservice, change_amount)` 在大规模集群中可能面临显著的可扩展性挑战。

**师兄补充：** 还没有做。

> 状态：待办。

---

## ② 第一组 · 意见 3 —— Lyapunov 贡献的消融

**原审稿意见：** 建议增加消融实验，定量刻画 Lyapunov 优化在框架中的具体贡献——例如设计一个奖励函数中**不含 Lyapunov 项**、改用“时延+成本加权”更直观奖励的变体。

**师兄补充：**
- **需要调整**，因为现在“有没有 Lyapunov 效果一样”，无法体现差异。
- 要像原来那个实验一样，体现出 Lyapunov 对**成本的控制**和**长期预算的平稳**。
- **单独出折线图。**

---

## ③ 第二组 · 意见 3 —— 预测 / 历史数据机制

**原审稿意见：** 基于 DRL 的弹性策略严重依赖到达率预测，但预测机制未充分评估。论文用历史数据做预测，却未说明预测模型、精度，以及预测误差对弹性性能的影响；建议在预测噪声下做敏感性分析。

**师兄补充：**
- 现在做的是“去掉历史数据”，**缺一个“已知当前数据（理想状态）”的对照**。
- 这个**单独出折线图给审稿人看**，因为不适合放在正文中。

---

## ④ 第二组 · 意见 5 —— 训练开销 / 收敛时间

**原审稿意见：** 论文未讨论 MDRL 模块的训练开销或收敛时间。在实时边缘场景下，DRL 训练与推理的计算代价和时间是关键，应量化以评估可部署性。

**师兄补充：**
- 需要补充**收敛时间**和**决策时间**（测试时生成每一步决策所花费的时间）。
- 这是一个**简单的表**。

---

## ⑤ 第二组 · 意见 6 —— 部署与路由联合优化解耦

**原审稿意见：** 部署与路由的联合优化被解耦为独立阶段 (MFFD + MDRL)，可能导致次优解；可探索端到端可训练架构，在单一 DRL 框架内联合优化两者。

**师兄补充（思路）：**
- 现在已经做了一个“**初始为 random**”的对照。
- 对比最后**收敛到的效果**和**收敛时间**。
- 测试时，random 部署的**前几个时隙**应该比 MFFD 差，看一下这个区别明显不明显。
- **收敛效果和收敛时间出一个图；前几个时隙的效果图单独出。**

---

## ⑥ 通用要求 —— 消融实验数据集选择

**师兄补充：**
- 所有消融实验可以**只选一个数据集**（但最好是同一个，**不用 sin**）。
